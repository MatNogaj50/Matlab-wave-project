# Matlab wave project
The application requires the user to enter parameter values (the range is limited - however MATLAB displays the allowed boundaries). It calculates and displays the equation along with a short animation. For too high omega values, the animation may be slightly impaired due to rapid changes and undersampling in the generated function.
//the .wav file for the radio function is optional
